[color]
	ui = auto
[user]
	name = Guillermo A. Sanchez
	email = memo.sanchez@gmail.com
[core]
	excludesfiles = ~/.gitignore
	editor = "subl -w"
[alias]
plog = log --graph --all --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(bold white)— %an%C(reset)' --abbrev-commit
